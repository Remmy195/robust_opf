# A short cost-risk sweep: 3 points on the weight grid, one row each in
# efficient_frontier.csv. configs/ladder.conf runs the same idea at full scale.

case   = data/case_ACTIVSg200.m
metric = max_active_flow
stage  = baseline

weight_grid = 0, 1, 2

kappa = 2
eta   = 0.8
k_bar = 15

solver_dc = gurobi
threads   = 4

outdir = results/examples/frontier_sweep
