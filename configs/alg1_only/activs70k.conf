# Algorithm 1 only (stage=baseline) on activs70k, max_active_flow.
case   = data/case_ACTIVSg70k.m
metric = max_active_flow
stage  = baseline

kappa = 2
eta   = 0.8
k_bar = 15

weight_grid = 0

solver_dc = gurobi
solver_ac = knitro
threads   = 16

outdir = results
write_lp = true
