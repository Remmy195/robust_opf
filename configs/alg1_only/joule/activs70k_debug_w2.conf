# Debug: reproduce the infeasible solve at weight=2.0 lambda* on activs70k,
# joule_loss_max, with the solver's own console output turned on.
case   = data/case_ACTIVSg70k.m
metric = joule_loss_max
stage  = baseline

kappa = 2
eta   = 0.8
k_bar = 15

weight_grid = 2.0

solver_dc = gurobi
solver_ac = knitro
threads   = 16
solver_verbose = 1

outdir = results/debug
