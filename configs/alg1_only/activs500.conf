# Algorithm 1 only (stage=baseline) on activs500, max_active_flow.
case   = data/case_ACTIVSg500.m
metric = max_active_flow
stage  = baseline

kappa = 2
eta   = 0.8
k_bar = 15

weight_grid = 0, 0.25, 0.5, 1, 2, 4

solver_dc = gurobi
solver_ac = knitro
threads   = 16

outdir = results
